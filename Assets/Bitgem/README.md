# Finding Home 🏡

یک بازی کارتی ساخته شده با Unity که در آن بازیکنان می‌توانند کارت‌ها را با هم ترکیب کنند و بازی کنند.

## 📋 پیش‌نیازها

برای اجرای این پروژه به موارد زیر نیاز دارید:

- **Unity Editor** (نسخه 2021.3 یا بالاتر توصیه می‌شود)
- **Universal Render Pipeline (URP)** - از قبل در پروژه نصب شده است
- **TextMesh Pro** - از قبل در پروژه نصب شده است
- **Odin Inspector** - برای ویرایش آسان‌تر ScriptableObjects

## 🚀 نحوه اجرا

### مرحله 1: باز کردن پروژه

1. Unity Hub را باز کنید
2. روی دکمه **"Open"** یا **"Add"** کلیک کنید
3. پوشه اصلی پروژه را انتخاب کنید: `Finding-home`
4. پروژه به‌طور خودکار باز می‌شود

### مرحله 2: باز کردن صحنه اصلی

1. در پنجره Project، به مسیر زیر بروید:
   ```
   Assets/demo01.unity
   ```
2. روی فایل `demo01.unity` دوبار کلیک کنید
3. صحنه بازی باز می‌شود

### مرحله 3: اجرای بازی

1. دکمه **Play** (▶️) را در بالای Unity Editor بزنید
2. بازی شروع می‌شود و می‌توانید با کارت‌ها بازی کنید

## 🎮 نحوه بازی

### کنترل‌های بازی

- **کلیک روی کارت**: انتخاب کارت
- **Drag & Drop**: کشیدن و رها کردن کارت‌ها
- **ترکیب کارت‌ها**: یک کارت را روی کارت دیگری رها کنید تا ترکیب شوند

### سیستم کارت‌ها

بازی دارای چندین نوع کارت است:

- **Cargo (محموله)**: کارت‌های حمل و نقل
- **Pini**: کارت‌های مخصوص
- **Food (غذا)**: کارت‌های مواد غذایی
- **Raw Materials (مواد خام)**: مواد اولیه
- **Crafted Materials (مواد ساخته شده)**: مواد پردازش شده
- **Pollution (آلودگی)**: کارت‌های آلودگی

### سیستم ترکیب (Combination System)

- کارت‌ها را می‌توان با هم ترکیب کرد تا کارت‌های جدید ایجاد شوند
- هر کارت می‌تواند تعداد محدودی در ترکیب‌ها شرکت کند
- برخی کارت‌ها پس از ترکیب ارتقا می‌یابند

### زون‌های بازی

- **Shop Zone (منطقه فروشگاه)**: خرید کارت‌های جدید
- **Sell Zone (منطقه فروش)**: فروش کارت‌ها

## 📁 ساختار پروژه

```
Assets/
├── Art/                    # تصاویر و گرافیک‌های بازی
├── Cards/                  # ScriptableObjects مربوط به کارت‌ها
│   ├── AllCard.asset      # لیست همه کارت‌ها
│   ├── Cargo/             # کارت‌های Cargo
│   ├── Food/              # کارت‌های غذا
│   ├── RawMaterials/      # مواد خام
│   ├── CraftiedMaterials/ # مواد ساخته شده
│   └── Pollution/         # کارت‌های آلودگی
├── Script/                 # کدهای C#
│   ├── GamePlayManager.cs # مدیریت اصلی بازی
│   ├── CardDataSO.cs      # ساختار داده کارت‌ها
│   ├── View/              # کلاس‌های نمایشی
│   └── Cards/             # منطق کارت‌های مختلف
├── prefab/                 # Prefab های Unity
│   ├── Card.prefab        # پیش‌فرض کارت
│   ├── Cargo.prefab       # پیش‌فرض Cargo
│   └── Pini.prefab        # پیش‌فرض Pini
├── Material/               # متریال‌های Unity
├── Shader/                 # شیدرهای سفارشی
└── demo01.unity           # صحنه اصلی بازی
```

## 🛠️ توسعه بازی

### ساخت کارت جدید

1. در پوشه `Assets/Cards`، راست کلیک کنید
2. `Create > FindingHome > CardDataSo` را انتخاب کنید
3. مشخصات کارت را وارد کنید:
   - `type`: نام کارت
   - `sprite`: تصویر کارت
   - `category`: دسته‌بندی کارت
   - `value`: ارزش کارت
   - `maxCombinationUses`: تعداد دفعات قابل استفاده در ترکیب‌ها

### افزودن ترکیب جدید

1. کارت مورد نظر را باز کنید
2. در بخش `CreateBy`، ترکیب‌های جدید اضافه کنید
3. مشخص کنید کدام کارت‌ها با هم ترکیب می‌شوند

### تنظیمات دوربین

- دوربین از سیستم Cinemachine استفاده می‌کند
- می‌توانید در `CinemachineTopDownController.cs` تنظیمات را تغییر دهید

## 🎨 ویژگی‌های بصری

- از **Universal Render Pipeline (URP)** استفاده می‌کند
- شامل شیدر آب سفارشی (Stylised Water)
- سیستم Billboard برای نمایش کارت‌ها به سمت دوربین

## 📦 Asset های استفاده شده

- **Bitgem Stylised Water**: برای افکت‌های آب
- **Odin Inspector**: برای ویرایشگر سفارشی
- **TextMesh Pro**: برای نمایش متن با کیفیت بالا

## ⚙️ تنظیمات

### تنظیمات بازی

فایل `GamePlayManager.cs` تنظیمات اصلی بازی را کنترل می‌کند:

- مدیریت کارت‌ها (اضافه کردن، حذف کردن)
- سیستم ترکیب
- به‌روزرسانی نمایش کارت‌ها

### تنظیمات کارت

هر کارت دارای تنظیمات زیر است:

- `Position`: موقعیت کارت در دنیا
- `BottomCardId` و `TopCardId`: برای سیستم stack کردن
- `ProcessTime`: برای کارت‌هایی که نیاز به زمان دارند

## 🐛 عیب‌یابی

### بازی اجرا نمی‌شود

- مطمئن شوید همه پکیج‌ها نصب شده‌اند
- منوی `Window > Package Manager` را چک کنید
- پروژه را یکبار Close و دوباره Open کنید

### کارت‌ها نمایش داده نمی‌شوند

- مطمئن شوید `AllCardSo` به `GamePlayManager` اتصال داده شده
- چک کنید که Prefab های کارت‌ها موجود باشند

### خطاهای Odin Inspector

- Odin Inspector یک Asset پولی است
- اگر ندارید، می‌توانید بدون آن هم توسعه بدهید (فقط Inspector ساده‌تر می‌شود)

## 📝 نکات مهم

1. **همیشه Scene را Save کنید**: `Ctrl+S` یا `Cmd+S`
2. **قبل از تغییرات بزرگ Backup بگیرید**
3. **از Git برای مدیریت نسخه استفاده کنید**

## 🤝 مشارکت

برای توسعه این پروژه:

1. کدهای جدید را به پوشه `Assets/Script/` اضافه کنید
2. از نام‌گذاری واضح استفاده کنید
3. کامنت‌های مناسب بگذارید

## 📞 پشتیبانی

در صورت مشکل:

1. فایل‌های Log را چک کنید: `Logs/` folder
2. Console Unity را بررسی کنید
3. مستندات Unity را مطالعه کنید: [docs.unity3d.com](https://docs.unity3d.com)

---

**موفق باشید! 🎉**

