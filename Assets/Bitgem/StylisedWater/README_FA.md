# 🌊 Stylised Water - راهنمای استفاده به فارسی

**Stylised Water** یک ابزار حرفه‌ای از Bitgem برای ساخت آب استایلیزه شده (کارتونی/هنری) در Unity با استفاده از Universal Render Pipeline (URP) است.

---

## 📋 فهرست مطالب

1. [نصب و راه‌اندازی](#نصب-و-راهاندازی)
2. [استفاده سریع](#استفاده-سریع)
3. [کامپوننت‌های اصلی](#کامپوننتهای-اصلی)
4. [آموزش گام‌به‌گام](#آموزش-گامبهگام)
5. [تنظیمات پیشرفته](#تنظیمات-پیشرفته)
6. [نکات و ترفندها](#نکات-و-ترفندها)

---

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

این ابزار از قبل در پروژه شما نصب شده است، اما نیاز دارید:

- ✅ **Unity 2021.3 یا بالاتر**
- ✅ **Universal Render Pipeline (URP)** نصب باشد
- ✅ **Shader Graph** فعال باشد

### بررسی نصب

برای بررسی نصب صحیح، این مسیرها را چک کنید:

```
Assets/Bitgem/StylisedWater/
├── URP/
│   ├── Examples/          ← صحنه‌های نمونه
│   ├── Materials/         ← متریال‌های آماده
│   ├── Scripts/           ← اسکریپت‌های ابزار
│   ├── Shaders/           ← شیدرهای آب
│   └── Textures/          ← تکسچرهای موج
```

---

## ⚡ استفاده سریع

### روش 1: استفاده از صحنه نمونه (توصیه می‌شود برای شروع)

1. به مسیر زیر بروید:
   ```
   Assets/Bitgem/StylisedWater/URP/Examples/
   ```

2. فایل `Example-Scene-01.unity` را باز کنید

3. دکمه **Play** را بزنید تا ببینید آب چطوری کار می‌کنه

4. در حالت Play، اشیاء را در Scene بررسی کنید تا یاد بگیرید چطور ست‌آپ شده

### روش 2: ساخت آب از صفر

1. **یک GameObject خالی بسازید**:
   - راست‌کلیک در Hierarchy
   - `Create Empty` → نام بذارید: `MyWater`

2. **کامپوننت Water Volume اضافه کنید**:
   - GameObject را انتخاب کنید
   - در Inspector، `Add Component` کلیک کنید
   - جستجو کنید: `Water Volume Box`

3. **تنظیمات پایه**:
   - **Dimensions**: اندازه آب (مثلا X=50, Y=5, Z=50)
   - **Tile Size**: اندازه کاشی‌ها (پیشنهاد: 1)

4. **متریال اضافه کنید**:
   - یک متریال از پوشه `Materials/` را به کامپوننت `Mesh Renderer` بکشید
   - پیشنهاد: `example-water-01.mat`

5. **تکمیل!** آب شما آماده است! 🎉

---

## 🧩 کامپوننت‌های اصلی

### 1. Water Volume Base

کلاس پایه برای همه انواع آب.

**ویژگی‌ها:**

- **Tile Size**: اندازه هر کاشی آب (0.1 تا 100)
- **Include Faces**: کدام دیوارهای آب نمایش داده بشن
  - ✅ NegX (چپ)
  - ✅ PosX (راست)
  - ✅ NegZ (جلو)
  - ✅ PosZ (عقب)
- **Include Foam**: روی کدام لبه‌ها کف داشته باشیم
- **Show Debug**: نمایش خطوط دیباگ
- **Realtime Updates**: به‌روزرسانی لحظه‌ای در Editor

### 2. Water Volume Box

سازنده یک جعبه آب.

**پارامترها:**

```csharp
public Vector3 Dimensions = new Vector3(50, 5, 50);
```

- **Dimensions.x**: عرض آب
- **Dimensions.y**: ارتفاع آب
- **Dimensions.z**: عمق آب

**محدودیت‌ها:**

- حداکثر 100 کاشی در X
- حداکثر 50 کاشی در Y
- حداکثر 100 کاشی در Z

### 3. Water Volume Floater

باعث می‌شه اشیاء روی آب شناور بمونن!

**نحوه استفاده:**

1. شیء مورد نظر (مثلا یک قایق) را انتخاب کنید
2. کامپوننت `Watever Volume Floater` اضافه کنید
3. اگر چندین آب دارید، `WaterVolumeHelper` مربوطه را اتصال دهید
4. حالا شیء روی سطح آب می‌مونه!

### 4. Water Volume Helper

سیستم کمکی برای مدیریت چندین منطقه آب.

**قابلیت‌ها:**

- `GetHeight(Vector3 position)`: ارتفاع آب در یک موقعیت را برمی‌گردونه
- مدیریت چندین حجم آب به‌صورت همزمان

---

## 📖 آموزش گام‌به‌گام

### پروژه 1: دریاچه ساده

```plaintext
هدف: یک دریاچه زیبا با آب آبی روشن بسازید
```

**مراحل:**

1. **ساخت آب**:
   - GameObject خالی بسازید → نام: `Lake`
   - Add Component → `Water Volume Box`
   - Dimensions: `X=30, Y=3, Z=30`

2. **متریال دریاچه**:
   - از پوشه `Materials/` متریال `example-water-02.mat` را بکشید
   - یا یک متریال جدید بسازید و شیدر `WaterVolume-URP` را انتخاب کنید

3. **تنظیم دیوارها**:
   - `Include Faces`: همه را تیک بزنید (برای دیوارهای زیر آب)
   - `Include Foam`: همه را تیک بزنید (برای کف روی لبه‌ها)

4. **بهینه‌سازی**:
   - `Tile Size = 2` (برای دریاچه بزرگ‌تر، کاشی بزرگ‌تر)

**نتیجه**: یک دریاچه با کف دریا و لبه‌های زیبا! ✨

### پروژه 2: رودخانه

```plaintext
هدف: رودخانه‌ای که از بین محیط می‌گذره
```

**مراحل:**

1. **ساخت بخش‌های رودخانه**:
   - چندین `Water Volume Box` بسازید
   - هر کدام را در مسیر رودخانه قرار دهید
   - Dimensions: `X=5, Y=2, Z=20` (برای هر بخش)

2. **تنظیمات دیوارها**:
   - `Include Faces`: فقط `NegX` و `PosX` (فقط دیوارهای کناری)
   - `Include Foam`: فقط `NegX` و `PosX` (کف فقط روی کناره‌ها)

3. **اتصال بخش‌ها**:
   - بخش‌ها را طوری قرار دهید که به هم متصل بشن
   - از `Tile Size = 1` برای دقت بیشتر استفاده کنید

4. **افکت جریان**:
   - در Material، پارامتر Speed را افزایش دهید

**نتیجه**: رودخانه‌ای با جریان طبیعی! 🏞️

### پروژه 3: اقیانوس بزرگ

```plaintext
هدف: یک اقیانوس وسیع برای محیط بازی
```

**مراحل:**

1. **ساخت آب بزرگ**:
   - `Water Volume Box` با Dimensions: `X=100, Y=10, Z=100`
   - ⚠️ توجه: به محدودیت‌های MAX_TILES توجه کنید

2. **بهینه‌سازی**:
   - `Tile Size = 5` یا `10` (برای کاهش تعداد کاشی‌ها)
   - `Include Faces`: فقط دیوارهای مورد نیاز

3. **متریال اقیانوس**:
   - از `example-water-03.mat` استفاده کنید
   - یا تنظیمات موج را افزایش دهید

4. **اضافه کردن قایق شناور**:
   - یک GameObject قایق بسازید
   - کامپوننت `Watever Volume Floater` اضافه کنید
   - حالا قایق روی امواج می‌رقصه! ⛵

---

## ⚙️ تنظیمات پیشرفته

### ویرایش شیدر آب

شیدر اصلی در مسیر زیر است:

```
Assets/Bitgem/StylisedWater/URP/Shaders/WaterVolume-URP.shadergraph
```

**پارامترهای قابل تنظیم در Material**:

| پارامتر | توضیح | مقدار پیشنهادی |
|---------|-------|----------------|
| **Base Color** | رنگ اصلی آب | آبی روشن |
| **Wave Scale** | مقیاس امواج | 1-5 |
| **Wave Speed** | سرعت حرکت امواج | 0.5-2 |
| **Foam Color** | رنگ کف | سفید |
| **Foam Amount** | میزان کف | 0.1-0.5 |
| **Transparency** | شفافیت آب | 0.5-0.9 |

### استفاده از چندین منطقه آب

برای داشتن چندین حجم آب:

```csharp
// در اسکریپت خودتون:
WaterVolumeHelper helper = FindObjectOfType<WaterVolumeHelper>();
float? waterHeight = helper.GetHeight(myPosition);

if (waterHeight != null)
{
    Debug.Log("ارتفاع آب: " + waterHeight.Value);
}
```

### ساخت آب سفارشی

می‌تونید از `WaterVolumeBase` ارث‌بری کنید:

```csharp
using Bitgem.VFX.StylisedWater;
using UnityEngine;

[AddComponentMenu("MyGame/Custom Water")]
public class MyCustomWater : WaterVolumeBase
{
    protected override void GenerateTiles(ref bool[,,] _tiles)
    {
        // شکل دلخواه خودتون رو اینجا بسازید
        // مثلا یک دایره:
        int centerX = MAX_TILES_X / 2;
        int centerZ = MAX_TILES_Z / 2;
        int radius = 20;

        for (int x = 0; x < MAX_TILES_X; x++)
        {
            for (int z = 0; z < MAX_TILES_Z; z++)
            {
                float distance = Vector2.Distance(
                    new Vector2(x, z),
                    new Vector2(centerX, centerZ)
                );

                if (distance <= radius)
                {
                    _tiles[x, 0, z] = true;
                }
            }
        }
    }
}
```

---

## 💡 نکات و ترفندها

### بهینه‌سازی Performance

1. **Tile Size را افزایش دهید**:
   - برای آب‌های بزرگ از Tile Size بالاتر (2-10) استفاده کنید
   - تعداد کاشی‌ها = Dimensions / Tile Size

2. **دیوارهای غیرضروری را خاموش کنید**:
   - اگر آب در گوشه نقشه است، دیوارهای بیرونی نیاز نیست

3. **از Realtime Updates فقط در Edit Mode استفاده کنید**:
   - در زمان بازی خاموش باشد

### مشکلات رایج و راه‌حل

| مشکل | راه‌حل |
|------|--------|
| آب نمایش داده نمی‌شود | چک کنید Material و Mesh Renderer وجود دارند |
| کف (Foam) نمایش داده نمی‌شود | `Include Foam` را فعال کنید |
| آب خیلی کند است | Tile Size را افزایش دهید یا Dimensions را کاهش دهید |
| شیء روی آب نمی‌ماند | کامپوننت `Watever Volume Floater` را اضافه کنید |
| آب شفاف نیست | در Material، Transparency را تنظیم کنید |

### ترفندهای بصری

1. **آب کم‌عمق**:
   - Base Color: فیروزه‌ای روشن
   - Transparency: بالا (0.8-0.9)

2. **آب عمیق**:
   - Base Color: آبی تیره
   - Transparency: پایین (0.3-0.5)

3. **آب آلوده/سمی**:
   - Base Color: سبز یا قهوه‌ای
   - Foam Color: زرد یا سبز روشن

4. **آب جادویی**:
   - Base Color: بنفش یا صورتی
   - افزودن Emission در Material

---

## 📁 فایل‌های مهم

```
StylisedWater/URP/
├── Examples/
│   └── Example-Scene-01.unity          ← صحنه نمونه کامل
│
├── Materials/
│   ├── example-water-01.mat            ← آب آبی روشن
│   ├── example-water-02.mat            ← آب دریاچه
│   └── example-water-03.mat            ← آب اقیانوس
│
├── Scripts/Bitgem/VFX/StylisedWater/
│   ├── WaterVolumeBase.cs              ← کلاس پایه
│   ├── WaterVolumeBox.cs               ← آب مکعبی
│   ├── WaterVolumeHelper.cs            ← سیستم کمکی
│   └── WateverVolumeFloater.cs         ← شناور کردن اشیاء
│
├── Shaders/
│   └── WaterVolume-URP.shadergraph     ← شیدر اصلی آب
│
└── Textures/
    └── waves_n.png                     ← تکسچر نرمال امواج
```

---

## 🎓 مثال‌های کاربردی

### مثال 1: سیستم شنای کاراکتر

```csharp
using UnityEngine;
using Bitgem.VFX.StylisedWater;

public class PlayerSwimming : MonoBehaviour
{
    private WaterVolumeHelper waterHelper;

    void Start()
    {
        waterHelper = FindObjectOfType<WaterVolumeHelper>();
    }

    void Update()
    {
        float? waterHeight = waterHelper?.GetHeight(transform.position);

        if (waterHeight != null && transform.position.y < waterHeight.Value)
        {
            // کاراکتر در آب است!
            StartSwimming();
        }
        else
        {
            StopSwimming();
        }
    }

    void StartSwimming()
    {
        // انیمیشن شنا، کاهش سرعت و...
        Debug.Log("Swimming!");
    }

    void StopSwimming()
    {
        // حالت عادی
    }
}
```

### مثال 2: خسارت آب

```csharp
public class DamageInWater : MonoBehaviour
{
    public float damagePerSecond = 5f;
    private WaterVolumeHelper waterHelper;

    void Start()
    {
        waterHelper = FindObjectOfType<WaterVolumeHelper>();
    }

    void Update()
    {
        float? waterHeight = waterHelper?.GetHeight(transform.position);

        if (waterHeight != null && transform.position.y < waterHeight.Value)
        {
            // آسیب به بازیکن در آب
            TakeDamage(damagePerSecond * Time.deltaTime);
        }
    }

    void TakeDamage(float damage)
    {
        Debug.Log("Taking water damage: " + damage);
    }
}
```

### مثال 3: افکت پاشش آب

```csharp
public class WaterSplash : MonoBehaviour
{
    public GameObject splashEffect;
    private WaterVolumeHelper waterHelper;
    private bool wasInWater = false;

    void Start()
    {
        waterHelper = FindObjectOfType<WaterVolumeHelper>();
    }

    void Update()
    {
        float? waterHeight = waterHelper?.GetHeight(transform.position);
        bool isInWater = waterHeight != null && transform.position.y < waterHeight.Value;

        // تشخیص ورود به آب
        if (isInWater && !wasInWater)
        {
            // ایجاد افکت پاشش
            Vector3 splashPos = new Vector3(
                transform.position.x,
                waterHeight.Value,
                transform.position.z
            );
            Instantiate(splashEffect, splashPos, Quaternion.identity);
        }

        wasInWater = isInWater;
    }
}
```

---

## 🚀 گام بعدی

حالا که با Stylised Water آشنا شدید:

1. ✅ صحنه نمونه را باز کنید و تست کنید
2. ✅ یک آب ساده در پروژه خودتون بسازید
3. ✅ با متریال‌ها و تنظیمات بازی کنید
4. ✅ یک شیء شناور اضافه کنید
5. ✅ یک سیستم تعامل با آب بسازید

---

## 📞 منابع بیشتر

- **مستندات Unity URP**: [docs.unity3d.com/Packages/com.unity.render-pipelines.universal](https://docs.unity3d.com/Packages/com.unity.render-pipelines.universal)
- **Shader Graph**: [docs.unity3d.com/Packages/com.unity.shadergraph](https://docs.unity3d.com/Packages/com.unity.shadergraph)
- **Bitgem**: [bitgem.co.uk](https://bitgem.co.uk)

---

## 🎉 تبریک!

شما الان می‌تونید آب‌های زیبا و استایلیزه شده در Unity بسازید!

اگر سوالی داشتید یا به مشکل خوردید، Console Unity رو چک کنید یا به مستندات رسمی مراجعه کنید.

**موفق باشید! 💧✨**

